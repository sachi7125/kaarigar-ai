# --- API / backend ---
fastapi>=0.111
uvicorn[standard]>=0.30
sqlalchemy>=2.0
psycopg2-binary>=2.9
pydantic>=2.7
python-dotenv>=1.0
jinja2>=3.1            # public storefront / listing page rendering
requests>=2.31

# --- AI pipelines ---
onnxruntime>=1.17      # runs u2netp for background removal directly (no rembg wrapper:
                       #   rembg drags in pymatting+numba whose JIT stalls for minutes)
opencv-python-headless==4.10.0.84   # PIN to 4.x: 5.0.0 imports in ~25s on arm64
pillow>=10.3
faster-whisper>=1.0   # whisper.cpp-style inference (transcription)
google-generativeai>=0.7   # Gemini free tier (description AND translation — see D11)
# NO torch / transformers / IndicTrans2: `import transformers` stalled ~20 min on the
# anaconda-based venv (enumerates every dist on sys.path). Translation is a passthrough;
# Gemini does regional->EN+HI directly. Optional real MT lives behind KAARIGAR_MT=indictrans2.

pyttsx3>=2.90          # offline TTS (server-side); flutter_tts on device

# --- pricing ---
xgboost>=2.0
shap>=0.45
scikit-learn>=1.4
numpy>=1.26
pandas>=2.2

# --- delivery toolkit ---
qrcode>=7.4
openpyxl>=3.1          # export bundle spreadsheets

streamlit>=1.35        # local playground / Day 6 demo

# --- dev ---
pytest>=8.0
httpx>=0.27           # test client
