# --- API / backend ---
fastapi>=0.111
uvicorn[standard]>=0.30
sqlalchemy>=2.0
psycopg2-binary>=2.9
pydantic>=2.7
python-dotenv>=1.0
jinja2>=3.1            # public storefront / listing page rendering
requests>=2.31

# --- AI pipelines ---
rembg>=2.0             # U2-Net background removal
onnxruntime>=1.17      # rembg inference backend
opencv-python>=4.9
pillow>=10.3
faster-whisper>=1.0   # whisper.cpp-style inference (transcription)
google-generativeai>=0.7   # Gemini free tier (description)
# indictrans2 is installed from source per its repo; pin the checkpoint in models/

pyttsx3>=2.90          # offline TTS (server-side); flutter_tts on device

# --- pricing ---
xgboost>=2.0
shap>=0.45
scikit-learn>=1.4
numpy>=1.26
pandas>=2.2

# --- delivery toolkit ---
qrcode>=7.4
openpyxl>=3.1          # export bundle spreadsheets

streamlit>=1.35        # local playground / Day 6 demo

# --- dev ---
pytest>=8.0
httpx>=0.27           # test client
