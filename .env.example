# Copy to .env and fill in. .env is git-ignored — never commit real keys.

# Gemini (bilingual listing description, step 3). Free key: https://aistudio.google.com/apikey
GEMINI_API_KEY=

# Optional: whisper.cpp server for transcription (else on-device faster-whisper is used).
# KAARIGAR_WHISPER_SERVER=http://localhost:8080
